<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- Annotation 메모리할당 -->
	<context:component-scan base-package="com.sist.*"/>
	<!-- 
		@Component
		public class MovieManager  => movieManager new MovieManager()
		
		@Controller 
		public class MovieController => MovieController new MovieController()
		
		@Autowired
		private MovieManager mgr;
	 -->
	<!-- 
		메모리할당
		@Component, 
		@Repository, 
		@Service,
		@Controller, 
		@RestController, 
		@ControllerAdvice, 
 		@Configuration
 		
 		주입
 		@Required, @Autowired, @PostConstruct, @PreDestroy, @Resource
	 -->
	<!-- 실행 => 데이터를 JSP로 전송 : ViewResolver(경로명,확장자) -->
	<!-- 
		 .do ->( DispatcherServlet -> HandlerMapping ) -> Model(@controller)
		 -> DAO(Manager) -> Model(@Controller) -> ( DispatcherServlet -> ViewResolver ) -> View(JSP)
	 -->
	
	<bean id="viewResolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver"
		p:prefix="/"
		p:suffix=".jsp" />	 
</beans>
